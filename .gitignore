# build output
dist/
.astro/

# deps
node_modules/

# logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.production

# os / editors
.DS_Store
.vscode/
.idea/

# NOTE: content imported by scripts/import-from-sources.ts (cli-commands,
# api, changes) is committed intentionally so the GitHub Pages build has
# everything it needs. Run `pnpm sync` locally before committing any change
# that should surface in those sections.
